name: petro-data

on:
  push:
    branches: [staging]

jobs:
  Mooyi-Survey-Ms:
    runs-on: ubuntu-latest

    steps:
      - name: Setup all dependencies and deploy to staging
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          key: ${{ secrets.SSH_KEY }}
          username: ${{ secrets.SSH_USERNAME }}
          script: 'cd ${{secrets.PETRO-DATA_DIR}} && git reset --hard origin/staging && git pull origin staging && npm install && npm run build && npm run start:prod'
